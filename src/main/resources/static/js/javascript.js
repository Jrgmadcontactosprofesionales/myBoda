//    $("document").ready(function() {
//        let href = $(this).attr("href");
//        if ($(href).match(/^.*nuevoAsistente$/)) {
//            $("title,h2").text("Añadir asistente");
//        }
//        });

/* document.window.addEventListener('ready',determineHref());

let href = window.location.href;
let title = document.getElementsByTagName("title");
let h2 = document.getElementsByTagName("h2");

function determineHref(href) {
    let text;
    if (href = 'http://localhost:8090/nuevoAsistente') {
        text = 'Añadir asistente';
    } else {
        text = 'Editar asistente';
    }
    title.innerHTML=text;
    h2.innerHTML=text;
} */